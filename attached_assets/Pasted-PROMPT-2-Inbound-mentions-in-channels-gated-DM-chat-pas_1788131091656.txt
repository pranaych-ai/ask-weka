PROMPT 2 — Inbound: @mentions in channels, gated DM chat (paste after Prompt 1 is done)
Inbound DM chat already works in backend/slack_app.py — keep its security model exactly as is (signature verification on the raw body, 5-minute replay window, event-id dedup, bot/subtype filtering, email resolution via users.info gated to WEKA domains, per-channel ordering locks, persistence + audit identical to web chat). Make these changes:

Config from the admin portal. slack_app.py reads the bot token and signing secret through the Prompt 1 integration row (with env fallback) instead of only os.environ. Signature verification uses the stored signing secret. As defense in depth, check the payload's api_app_id against the stored app id and ignore mismatches (there is only one app, but a stale or foreign app should never be processed).
Feature gating. DM chat now requires the dm_chat feature to be admin-enabled. If a user DMs the bot while it's disabled, reply once with a short pointer ("Slack chat is currently turned off — use <Ask WEKA link>") and stay silent for repeats within the same day. DM chat does NOT require individual opt-in (messaging the bot first is the consent), but create/refresh the user's slack_user_prefs row with their Slack id on first contact so outbound features can reach them later.
@mentions (feature flag: mentions). Subscribe to app_mention. When someone mentions @Ask WEKA in a channel it's been invited to, treat the message text (minus the mention) as a question, resolve the sender's email exactly like DMs, run it through the same chat pipeline, and reply in the same thread. Channel questions get a fresh single-exchange context (do not attach channel threads to the user's DM conversation history), but persist and audit them the same way with via=slack-mention detail.
Slack-friendly answers. Convert the model's markdown to Slack mrkdwn (single-asterisk bold, no headings — bold lines instead), keep answers concise, and end with a link button to open Ask WEKA. While generating, add an eyes reaction to the user's message (reactions:write) and remove it when the answer posts.
Answer scoping unchanged: everything runs through build_system_prompt and the active KB sections — a Slack answer must never reveal anything the same employee couldn't get in the web app.
Log every inbound interaction (username, event type, success/failure) via log_event — event metadata only, never message text. Add tests for the gating and mention-parsing logic. Run pytest.